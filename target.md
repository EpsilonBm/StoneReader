# StoneReader 开发目标

## 概述

StoneReader是一个轻量化的阅读器，基于Python和PyQt6开发，开源并且可拓展。

## 目标

### 功能

- 格式：可以支持包括epub、mobi、azw3、txt等包括二进制与非二进制文件的多种格式，首要保证各种主流电子书的格式支持
- 词典：可以实现取词翻译、取段翻译、沉浸式翻译（即翻译内容在原文段后，嵌入其中），使用在线翻译工具
- 笔记、书签和标记：对于电子书格式和纯二进制格式支持，笔记是对内容的注释，可以是对某段内容，也可以添加在某个位置；书签是对某个位置的标记；标记是对某段内容的高亮、圈画等等；以上内容需要均支持管理和导出。
- 搜索：可以搜索内容，并支持内容高亮
- 标签：可以根据标签对书籍进行分类管理
- 已读：支持将已经读完的书籍归档
- 进度：支持记录阅读进度，并且可以在不同设备上同步
- 设置：所有的设置都应该在本地中保存，单独放在一个文件夹下，每次打开应用加载相关配置
- ...

### 界面

- 风格化：界面风格简洁、现代，使用自定义的控件和舒适的配色方案，支持用户自定义主题和配色方案
- 文字：在界面正中是文字显示区域，支持调整字体、字号、字颜色、行距、行长度、背景颜色等，并且调整逻辑简单，方便操作。可以一键切换夜间模式和日间模式，两种模式的主题可以根据前面用户的调整而保存
- 目录：可以显示目录，并且可以跳转到目录中的章节
- 书籍：支持以几种方式（如列表、格子等）显示最近书籍（使用软件打开过的书籍）、本地书籍（在本地中扫描到的书籍，指定扫描范围之后可以扫描，可以调整）、标签分类之后的书籍等。
- ...

### 代码风格

- 模块化：代码应该被组织成模块，每个模块负责一个特定的功能或一组相关的功能。这有助于提高代码的可维护性和可读性。
- 注释：在每一个模块、函数、变量处要解释作用等，在重要的代码段中也要添加注释
- 版本管理：使用git组织版本
- 测试：编写单元测试和集成测试，确保代码的正确性和稳定性
- 文档：编写清晰的文档，包括代码注释、使用说明和开发指南，以便其他开发者能够理解和使用代码
- 可迁移：代码应该能够在不同的平台和环境中运行，尽量避免使用特定于某个平台的功能或库，以提高代码的可迁移性。目前代码应当至少可以在Windows环境中运行，尽量保证Android中也可以
- ...

### 可拓展性

- 插件系统：设计一个插件系统，使得开发者可以轻松地为StoneReader添加新的功能或支持新的格式，而不需要修改核心代码。
- API：提供一个清晰的API，使得开发者可以通过API与StoneReader进行交互，获取书籍信息、阅读进度等数据，并且可以通过API控制

## 工作

### 每一次迭代的工作流程

- 设计阶段：确定软件的功能需求、界面设计和代码结构
- 开发阶段：根据每一次迭代的要求进行开发，编写代码实现功能
- 测试阶段：运行测试用例，修复发现的bug
- 审核阶段：将完成的工作添加到根目录下的docs/roadmap.md中，并且在README.md中添加使用说明，由我来审核工作结果并且告诉你需要改进的地方，最终完成
之后可以进行git提交，之后进入下一个迭代；否则回到第一步或者第二部再进行修改

### 具体步骤

#### 准备阶段

创建python虚拟环境，准备依赖管理方法，创建git仓库，管理.gitignore文件。

#### 迭代1

- 基于常见PyQt6的项目框架，完成项目的基础框架搭建，建立代码规范，在master分支下进行init commit，在本地仓库中创建一个dev分支。
- 接着完成一个基本界面的搭建。基本界面的主视图是一个书架界面，在左侧是展示选项列表，可以根据选项列表调整书架上要展示哪些书，详细的展示选项在下面补充；在中间是以两种方式展示的文件，一种是网格，每一个书籍由封面和名字组成，另一种是列表，最左侧是一个小的封面缩略图，中间是长的名称显示，右边是作者。合适的位置放“添加书籍”选项，比如在书架的末尾放上一个空白的、中间是一个加号的书籍，或者在书架的上方放上一个按钮，完成导入书籍的功能。关于展示选项：
  - 1）书架：默认视图，可以按照阅读时间顺序、添加时间顺序、书名顺序、作者顺序等展示所有书架上的书籍。
  - 2）标签：子选项是由用户在书籍上创建的标签，点击之后展示的是由书籍标签分类之后的书籍。这个标签由应用管理。
  - 3）最爱：这个可以看做是一个特殊的标签，但是不能和标签混在一起。
  - 4）已读：这个也可以看做是一个特殊的标签，已经读完的书归档于此。
- 注意显示对屏幕的支持，需要先读取屏幕分辨率，适配不同大小的屏幕，调整界面布局和元素大小以适应不同的屏幕尺寸，保证在不同设备上都有良好的用户体验。

#### 迭代2

- 首先完成对TXT等二进制文件的支持。点击书架上的二进制文件之后可以进入阅读界面，在阅读界面的左上角可以退回到书架界面。
- 在阅读界面中央是文字部分，文字的显示需要支持调整字体、字号、字颜色、行距、行长度、背景颜色等。暂定这些调整在一个单独的调整窗口中完成。界面底部是一个可以拖动的阅读进度条，展示当前阅读进度并且可以拖动调整阅读位置。
- 完成基础的搜索功能，可以在阅读界面中点击搜索按钮，输入关键词之后可以在当前章节中高亮展示搜索结果，并且可以通过“下一个”按钮跳转到下一个搜索结果的位置。
- 一定要注意阅读界面的美观，使用风格化的界面设计。

#### 迭代3

- 完成对epub、azw3、mobi等主流电子书格式的支持。在阅读界面中文字分章节展示，在文字部分左右两侧有切换到上一章和下一章的按钮。
- 左侧边栏集中展示章节、书签、笔记和标记，在左侧边栏的上方有四个按钮可以切换侧边栏的显示方式。对于章节，显示章节目录，包括一级、二级目录等。点击章节可以跳转。其余部分均应当支持点击跳转。
- 完成书签、笔记和标记功能。书签是对某个位置的标记，笔记是对某段内容的注释；标记是对某段内容的高亮、圈画等等；以上内容需要均支持管理和导出。
- 左边栏支持折叠，折叠之后文字区域占据整个屏幕。带来沉浸式阅读体验。
